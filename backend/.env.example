# Anthropic API Key (optional - for LLM-powered job URL parsing)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# LinkedIn Session Cookie (optional - for authenticated LinkedIn job parsing)
# Without this, LinkedIn may block requests and show "requires authentication" message.
# To get this cookie:
#   1. Log into LinkedIn in your browser
#   2. Open DevTools (F12) → Application → Cookies → linkedin.com
#   3. Find and copy the value of the "li_at" cookie
# Note: This cookie expires periodically - refresh it if parsing stops working
LINKEDIN_LI_AT=your_linkedin_li_at_cookie_here

# Debug Mode (optional - for troubleshooting scraping issues)
# When enabled:
#   - Logs request/response details to console (status, redirects, headers, cookies)
#   - Saves raw HTML to backend/debug/last_response.html
#   - Adds diagnostic info to error messages
DEBUG_SCRAPING=false